// const express = require("express");
// const router = express.Router();
// const authenticate = require("../middlewares/authenticate");
// const ntfctionController = require("../controllers/ntfction-controller");

// router.get("/", authenticate, ntfctionController.getByNtfction);
// router.post("/", authenticate, ntfctionController.createNtfction);
// router.put("/:id", authenticate, ntfctionController.updateNtfction);
// router.delete("/:id", authenticate, ntfctionController.deleteNtfction);

// module.exports = router;
